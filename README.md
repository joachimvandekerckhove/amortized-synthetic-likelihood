# Amortized synthetic likelihood

Minimal reproducibility package for multivariate DDM emulators (`ddm3mv`, `ddm4mv`).

Training data in `data/` was generated with `n_theta=20000`, `n_rep=600`, `R=120`, `seed=42`.
